global:
  namespace: spring-clinic
  imagePullPolicy: IfNotPresent
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"
  configServer:
    url: "http://config-server:8888"
    enabled: true
services:
  admin:
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "admin-server-b15c163"
    containerPort: 9090
    service:
      type: ClusterIP
      port: 9090
      targetPort: 9090
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70

  apigateway:
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "api-gateway-14d9761"
    containerPort: 8080
    service:
      type: ClusterIP
      port: 8080
      targetPort: 8080
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70

  config:
    gitUri: https://github.com/KhacThien88/config-server-clinic.git
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "config-server-5e7552a"
    containerPort: 8888
    service:
      type: ClusterIP
      port: 8888
      targetPort: 8080
    resources:
      requests:
        memory: "256Mi"
        cpu: "250m"
      limits:
        memory: "256Mi"
        cpu: "250m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%

  customer:
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "customers-service-b264909"
    containerPort: 8081
    service:
      type: ClusterIP
      port: 8081
      targetPort: 8081
    resources:
      requests:
        memory: "512Mi"
        cpu: "256m"
      limits:
        memory: "512Mi"
        cpu: "256m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70

  discovery:
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "discovery-server-e798e73"
    containerPort: 8761
    service:
      type: ClusterIP
      port: 8761
      targetPort: 8761
    resources:
      requests:
        memory: "512Mi"
        cpu: "512m"
      limits:
        memory: "512Mi"
        cpu: "512m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70

  genai:
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "genai-service-8bbfae3"
    containerPort: 8084
    service:
      type: ClusterIP
      port: 8084
      targetPort: 8084
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70

  vets:
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "vets-service-951d802"
    containerPort: 8083
    service:
      type: ClusterIP
      port: 8083
      targetPort: 8083
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70

  visits:
    enabled: true
    replicaCount: 1
    image:
      repository: ktei8htop15122004/clinic-microservices
      tag: "visits-service-f84808b"
    containerPort: 8082
    service:
      type: ClusterIP
      port: 8082
      targetPort: 8082
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70
  zipkin:
    baseUrl: "http://clinic-staging-zipkin.spring-clinic-staging.svc.cluster.local:9411/"
    enabled: true
    replicaCount: 1
    image: 
      repository: openzipkin/zipkin
      tag: "latest"
    containerPort: 9411
    service:
      type: ClusterIP
      port: 9411
      targetPort: 9411
    resources:
      requests:
        memory: "128Mi"
        cpu: "128m"
      limits:
        memory: "128Mi"
        cpu: "128m"
    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 50%
        maxUnavailable: 25%
    hpa:
      enabled: true
      minReplicas: 1
      maxReplicas: 2
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
        - type: Resource
          resource:
            name: memory
            target:
              type: Utilization
              averageUtilization: 70
spec:
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"

ingress:
  enabled: true
  ingressClassName: nginx
  rules:
    - host: "testenv-pet-clinic.khacthienit.click"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: apigateway
                port:
                  number: 8080
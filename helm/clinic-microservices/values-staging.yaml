global:
  namespace: spring-clinic-staging
  imagePullPolicy: IfNotPresent
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"
services:
  admin:
    replicaCount: 1
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
  apigateway:
    replicaCount: 1
    resources:
      requests:
        memory: "200Mi"
        cpu: "200m"
      limits:
        memory: "250Mi"
        cpu: "250m"
  config:
    gitUri: https://github.com/KhacThien88/config-server-clinic.git
    replicaCount: 1
    resources:
      requests:
        memory: "256Mi"
        cpu: "250m"
      limits:
        memory: "256Mi"
        cpu: "250m"
  customer:
    replicaCount: 1
    resources:
      requests:
        memory: "512i"
        cpu: "256m"
      limits:
        memory: "512Mi"
        cpu: "256m"
  discovery:
    replicaCount: 1
    resources:
      requests:
        memory: "512Mi"
        cpu: "512m"
      limits:
        memory: "512Mi"
        cpu: "512m"
  genai:
    replicaCount: 1
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
  vets:
    replicaCount: 1
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
  visits:
    replicaCount: 1
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
  zipkin:
    baseUrl: "http://clinic-staging-zipkin.spring-clinic-staging.svc.cluster.local:9411/"
    enabled: true
    replicaCount: 1
    resources:
      requests:
        memory: "256Mi"
        cpu: "256m"
      limits:
        memory: "256Mi"
        cpu: "256m"
spec:
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"
ingress:
  enabled: true
  ingressClassName: nginx
  rules:
    - host: "stagingenv-pet-clinic.khacthienit.click"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clinic-staging-apigateway
                port:
                  number: 8080